import QtQuick 1.1

import QtMobility.location 1.0

Rectangle {
	width: 800
	height: 600

	Position {
		/*
		* altitude  : double
		* altitudeValid : bool
		* latitude : double
		* latitudeValid : bool
		* longtitude : double

		* longtitudeValid : bool
		* speed : double
		* speedValid : bool
		* timestamp : date
		*/
	}
}
